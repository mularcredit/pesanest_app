"use client";

import Link from "next/link";
import Image from "next/image";
import { useState } from "react";
import { motion } from "framer-motion";
import { BiRightArrowAlt, BiCheck, BiStore, BiQr, BiWifi } from "react-icons/bi";
import { HiMenuAlt3, HiX } from "react-icons/hi";

export default function Home() {
  const [isMenuOpen, setIsMenuOpen] = useState(false);

  return (
    <div className="min-h-screen bg-gray-50 font-sans selection:bg-purple-500 selection:text-white overflow-hidden relative">
      {/* Global Ambient Background (Matches Login) */}
      {/* Global Ambient Background - Reference Design Match */}
      <div className="absolute inset-0 overflow-hidden pointer-events-none bg-[#f8f9fa]">
        {/* Main large central gradient - Pink/Red warmth */}
        <div className="absolute top-[-20%] right-[-10%] w-[1200px] h-[1200px] bg-[radial-gradient(circle,rgba(240,180,180,0.5)_0%,rgba(255,255,255,0)_70%)] blur-3xl mix-blend-multiply opacity-80"></div>

        {/* Secondary purple wash for depth */}
        <motion.div
          animate={{
            scale: [1, 1.1, 1],
            opacity: [0.4, 0.6, 0.4],
          }}
          transition={{ duration: 10, repeat: Infinity, ease: "easeInOut" }}
          className="absolute top-[10%] right-[10%] w-[800px] h-[800px] bg-purple-200/50 rounded-full blur-3xl mix-blend-multiply"
        />

        {/* Bottom left balancer */}
        <motion.div
          animate={{
            scale: [1, 1.2, 1],
            opacity: [0.3, 0.5, 0.3],
          }}
          transition={{ duration: 15, repeat: Infinity, ease: "easeInOut" }}
          className="absolute -bottom-[20%] -left-[10%] w-[1000px] h-[1000px] bg-gray-100/50 rounded-full blur-3xl mix-blend-multiply"
        />
      </div>

      {/* Floating Spheres */}
      <motion.div
        animate={{ y: [0, -20, 0] }}
        transition={{ duration: 5, repeat: Infinity, ease: "easeInOut" }}
        className="absolute top-1/4 left-[15%] w-16 h-16 bg-gradient-to-br from-white to-gray-200 rounded-full shadow-[0_10px_30px_rgba(0,0,0,0.1)] hidden lg:block z-0"
      />
      <motion.div
        animate={{ y: [0, 30, 0] }}
        transition={{ duration: 7, repeat: Infinity, ease: "easeInOut", delay: 1 }}
        className="absolute bottom-1/4 right-[15%] w-24 h-24 bg-gradient-to-t from-gray-100 to-white rounded-full shadow-[0_15px_35px_rgba(0,0,0,0.05)] hidden lg:block z-0"
      />

      {/* Navbar */}
      <nav className="fixed top-0 left-0 right-0 z-50 backdrop-blur-md bg-white/60 border-b border-white/20">
        <div className="max-w-7xl mx-auto px-6 py-4 flex justify-between items-center">
          <Link href="/" className="flex items-center gap-1">
            <Image
              src="/capitalpay.png"
              alt="Capital Pay"
              width={150}
              height={35}
              className="h-8 w-auto object-contain"
            />
          </Link>

          {/* Desktop Links */}
          <div className="hidden md:flex items-center gap-8 text-sm font-medium text-gray-600">
            <Link href="#" className="hover:text-black transition-colors">Product</Link>
            <Link href="#" className="hover:text-black transition-colors">Use cases</Link>
            <Link href="#" className="hover:text-black transition-colors">Developers</Link>
            <Link href="#" className="hover:text-black transition-colors">Pricing</Link>
          </div>

          {/* CTA & Mobile Menu */}
          <div className="flex items-center gap-4">
            <Link
              href="/dashboard"
              className="hidden md:flex items-center justify-center px-6 py-2.5 rounded-full bg-black text-white text-sm font-bold hover:bg-gray-800 transition-all hover:scale-105 active:scale-95 shadow-lg shadow-gray-200"
            >
              Dashboard
            </Link>
            <button
              className="md:hidden text-2xl text-gray-900"
              onClick={() => setIsMenuOpen(!isMenuOpen)}
            >
              {isMenuOpen ? <HiX /> : <HiMenuAlt3 />}
            </button>
          </div>
        </div>

        {/* Mobile Menu Dropdown */}
        {isMenuOpen && (
          <motion.div
            initial={{ opacity: 0, y: -20 }}
            animate={{ opacity: 1, y: 0 }}
            className="md:hidden absolute top-full left-0 right-0 bg-white border-b border-gray-100 p-6 flex flex-col gap-4 shadow-xl"
          >
            <Link href="#" className="text-lg font-medium text-gray-900">Product</Link>
            <Link href="#" className="text-lg font-medium text-gray-900">Use cases</Link>
            <Link href="#" className="text-lg font-medium text-gray-900">Developers</Link>
            <Link href="#" className="text-lg font-medium text-gray-900">Pricing</Link>
            <Link
              href="/dashboard"
              className="mt-4 flex items-center justify-center w-full px-6 py-3 rounded-full bg-black text-white text-base font-bold"
            >
              Dashboard
            </Link>
          </motion.div>
        )}
      </nav>

      {/* Hero Section */}
      <main className="pt-32 pb-16 px-6 max-w-7xl mx-auto relative z-10">
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-16 items-center">

          {/* Left Content */}
          <div className="relative z-10 max-w-xl">
            <motion.h1
              initial={{ opacity: 0, y: 20 }}
              animate={{ opacity: 1, y: 0 }}
              transition={{ duration: 0.8, ease: "easeOut" }}
              className="text-6xl md:text-7xl lg:text-8xl font-bold tracking-tight text-gray-900 leading-[1.1] mb-8"
            >
              Expense <br />
              <span className="font-light text-gray-400">management for</span> <br /> any business<span className="text-purple-600">.</span>
            </motion.h1>

            <motion.p
              initial={{ opacity: 0, y: 20 }}
              animate={{ opacity: 1, y: 0 }}
              transition={{ duration: 0.8, delay: 0.2, ease: "easeOut" }}
              className="text-lg text-gray-600 mb-10 leading-relaxed max-w-md"
            >
              We bring together everything that's required to control spend, automate approvals, and track budgets globally.
            </motion.p>



            <motion.div
              initial={{ opacity: 0 }}
              animate={{ opacity: 1 }}
              transition={{ delay: 0.8, duration: 1 }}
              className="mt-12 flex items-center gap-3"
            >
              <button className="w-12 h-12 rounded-full border border-gray-200 bg-white flex items-center justify-center hover:bg-gray-50 transition-colors shadow-sm text-gray-400 hover:text-gray-900">
                <BiRightArrowAlt className="text-xl rotate-90" />
              </button>
            </motion.div>
          </div>

          {/* Right Content - 3D Laptop & Card Composition */}
          <div className="relative h-[700px] hidden lg:flex items-center justify-center perspective-[2000px] -mt-20">

            {/* 3D Laptop Construction */}
            <motion.div
              initial={{ rotateY: -20, rotateX: 10, opacity: 0 }}
              animate={{ rotateY: -15, rotateX: 5, opacity: 1 }}
              transition={{ duration: 1.2, ease: "easeOut" }}
              className="relative w-[600px] h-[360px]"
              style={{ transformStyle: 'preserve-3d' }}
            >
              {/* Screen (Lid) */}
              <div className="absolute top-0 left-0 w-full h-[340px] bg-gray-900 rounded-[1.5rem] p-1 shadow-2xl border-[4px] border-gray-200 transform origin-bottom transition-transform duration-500"
                style={{ transform: 'rotateX(0deg) translateZ(0px)' }}>
                {/* Inner Screen Bezel */}
                <div className="w-full h-full bg-gray-950 rounded-[1.2rem] overflow-hidden relative">
                  {/* Wallpaper / Dashboard Content */}
                  <div className="absolute inset-0 bg-gray-100 flex flex-col p-6 overflow-hidden">
                    {/* Fake Header */}
                    <div className="w-full flex justify-between items-center mb-6">
                      <div className="flex gap-4">
                        <div className="w-3 h-3 rounded-full bg-red-400"></div>
                        <div className="w-3 h-3 rounded-full bg-yellow-400"></div>
                        <div className="w-3 h-3 rounded-full bg-green-400"></div>
                      </div>
                      <div className="w-32 h-2 bg-gray-300 rounded-full"></div>
                    </div>

                    {/* Dashboard Grid */}
                    <div className="grid grid-cols-12 gap-4 h-full">
                      {/* Sidebar */}
                      <div className="col-span-3 h-full bg-white rounded-xl p-4 space-y-3">
                        <div className="w-full h-8 bg-purple-50 rounded-lg"></div>
                        <div className="w-3/4 h-2 bg-gray-100 rounded-full"></div>
                        <div className="w-1/2 h-2 bg-gray-100 rounded-full"></div>
                        <div className="w-full h-32 mt-4 bg-blue-50/50 rounded-lg"></div>
                      </div>
                      {/* Main Content */}
                      <div className="col-span-9 space-y-4">
                        <div className="flex gap-4 h-32">
                          <div className="flex-1 bg-white rounded-xl shadow-sm border border-gray-100 p-4">
                            <div className="w-8 h-8 bg-orange-100 rounded-full mb-2"></div>
                            <div className="w-16 h-4 bg-gray-200 rounded-md"></div>
                          </div>
                          <div className="flex-1 bg-white rounded-xl shadow-sm border border-gray-100 p-4">
                            <div className="w-8 h-8 bg-blue-100 rounded-full mb-2"></div>
                            <div className="w-16 h-4 bg-gray-200 rounded-md"></div>
                          </div>
                        </div>
                        <div className="w-full h-40 bg-white rounded-xl shadow-sm border border-gray-100 p-4">
                          <div className="w-full h-full bg-gradient-to-t from-purple-50 to-transparent rounded-lg border-b border-purple-100 flex items-end gap-2 px-2 pb-2">
                            <div className="w-1/6 h-[40%] bg-purple-200 rounded-t"></div>
                            <div className="w-1/6 h-[70%] bg-purple-300 rounded-t"></div>
                            <div className="w-1/6 h-[50%] bg-purple-200 rounded-t"></div>
                            <div className="w-1/6 h-[80%] bg-purple-400 rounded-t"></div>
                            <div className="w-1/6 h-[60%] bg-purple-300 rounded-t"></div>
                            <div className="w-1/6 h-[45%] bg-purple-200 rounded-t"></div>
                          </div>
                        </div>
                      </div>
                    </div>
                  </div>

                  {/* Screen Glare */}
                  <div className="absolute inset-0 bg-gradient-to-tr from-transparent via-white/10 to-transparent pointer-events-none"></div>
                </div>
              </div>

              {/* Base (Keyboard Deck) */}
              <div className="absolute -bottom-[20px] left-[-5%] w-[110%] h-[30px] bg-gray-100 rounded-b-[2rem] rounded-t-[4px] shadow-[0_25px_50px_-12px_rgba(0,0,0,0.25)] border-t border-gray-300 flex items-center justify-center transform"
                style={{ transform: 'rotateX(-90deg) translateZ(15px)' }}>
                {/* Front Lip */}
                <div className="w-32 h-1 bg-gray-300 rounded-full opacity-50"></div>
              </div>
            </motion.div>


            {/* Floating Glass Card (Reused exactly from Login) */}
            <motion.div
              initial={{ opacity: 0, x: 50, y: 50, rotateY: 10, rotateZ: -5 }}
              animate={{
                opacity: 1,
                x: 0,
                y: 0,
                rotateY: 0,
                rotateZ: 0
              }}
              transition={{
                duration: 1.2,
                ease: "easeOut",
                delay: 0.3
              }}
              className="absolute right-0 bottom-32 w-[320px] h-[200px] z-20"
              style={{ transformStyle: 'preserve-3d' }}
            >
              {/* Float Animation Wrapper */}
              <motion.div
                animate={{ y: [0, -15, 0] }}
                transition={{ duration: 6, repeat: Infinity, ease: "easeInOut" }}
                className="w-full h-full"
              >
                <div className="absolute inset-0 bg-white/40 backdrop-blur-xl border border-white/60 rounded-2xl shadow-[0_20px_60px_rgba(0,0,0,0.15)] p-6 text-gray-800 flex flex-col justify-between backdrop-filter">
                  <div className="flex justify-between items-start">
                    <BiWifi className="text-2xl rotate-90" />
                    <div className="text-xs font-bold tracking-widest uppercase opacity-50">Debit</div>
                  </div>
                  <div className="space-y-4">
                    <div className="flex gap-2">
                      <div className="h-2 w-8 bg-gray-400/20 rounded-full" />
                      <div className="h-2 w-4 bg-gray-400/20 rounded-full" />
                    </div>
                    <div className="text-xl font-mono tracking-wider opacity-80">•••• •••• •••• 4289</div>
                  </div>
                  {/* Card Sheen */}
                  <div className="absolute inset-0 bg-gradient-to-tr from-white/40 via-transparent to-transparent rounded-2xl pointer-events-none"></div>
                </div>
              </motion.div>

              {/* Decorative Glows around card */}
              <div className="absolute -top-12 -right-12 w-32 h-32 bg-purple-400/30 rounded-full blur-3xl -z-10 animate-pulse"></div>
              <div className="absolute -bottom-12 -left-12 w-32 h-32 bg-pink-400/30 rounded-full blur-3xl -z-10 animate-pulse" style={{ animationDelay: '1s' }}></div>
            </motion.div>
          </div>
        </div>


      </main>
    </div>
  );
}

